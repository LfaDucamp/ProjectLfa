using UnityEngine;
using System.Collections;

public class AutoDestruct2s : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 2);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
